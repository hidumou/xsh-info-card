  {% include 'research/_focus_line.html.j2' %}
  <footer class="supsub-footer"{% if footer_style %} style="{{ footer_style }}"{% endif %}>
    <div class="url">supsub.ai</div>
    <div class="disc">{{ disc | default('Information for reference, not investment advice.') }}</div>
    <div class="pg">{{ '%02d' % page_no }} / {{ '%02d' % total_pages }}</div>
  </footer>
